package me.williamhester.network;

/**
 * Since Voat's API was actually designed, we can wrap the entire API using Retrofit. This interface
 * contains all necessary endpoints that Retrofit will wrap.
 *
 * @author William Hester
 */
public interface VoatService {
}
